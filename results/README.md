# results

Store reproducible experiment outputs, observations, screenshots, benchmark summaries, logs, and generated result artifacts here.

Do not store secrets, credentials, private institutional information, or unnecessary build/cache files.

Where practical, include the command or procedure used to generate each result so that it can be reproduced.
