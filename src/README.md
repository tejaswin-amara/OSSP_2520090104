# src

This directory contains project source code, implementations, reusable components, and executable programs.

## Rules

- Keep source organized by feature/module.
- Use descriptive filenames.
- Do not commit credentials or local configuration.
- Put generated output in `results/`, not in source directories.
- Preserve meaningful commit history for individual contributions.
