# Task 2 Screenshot Evidence

Keep Task 2 screenshots in this folder.

Recommended files:

1. `01_file1_and_source.png` – `File1.txt` and the source program.
2. `02_file_copy_execution.png` – compilation and successful execution of `file_copy`.
3. `03_file2_verification.png` – `cat`/`cmp` verification showing that `File2.txt` matches `File1.txt`.

The documentation and source code are kept one level above this folder so evidence remains separate from the implementation.
