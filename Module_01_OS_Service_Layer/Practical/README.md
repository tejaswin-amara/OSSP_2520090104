# Practical Work

This folder contains work whose content is practical/lab based. Classification is based on the actual task content, not filenames.

## Current practical work

- `OSSP_Practical_Week_2.md` — process creation using `fork()`, `exec()`, and `wait()`, plus Linux commands used to study OS services and hardware resources.
- `OSSP_Practical_Week_3.md` — parent/child process creation using `fork()` and a process-state transition experiment covering RUNNING, WAITING, TERMINATING, and CHILD TERMINATED states.
